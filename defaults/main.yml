---
# defaults file for setup

setup_reboot: no
setup_timezone: America/Los_Angeles
setup_locale: en_US.UTF-8

# Ssh authorized hosts
setup_authorized_host_keys: []

# Ssh host key (instead of sshkeygen)
setup_ssh_host_key:
